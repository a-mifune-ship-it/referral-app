# Environment variables declared in this file are NOT automatically loaded by Prisma.
# Please add `import "dotenv/config";` to your `prisma.config.ts` file, or use the Prisma CLI with Bun
# to load environment variables from .env files: https://pris.ly/prisma-config-env-vars.

# PostgreSQL connection string.
# Local dev example (see README for setup): postgresql://postgres:localdev@localhost:5432/referral_app
# Vercel Postgres / Neon / Supabase: copy the connection string they provide.
DATABASE_URL="postgresql://postgres:localdev@localhost:5432/referral_app"
